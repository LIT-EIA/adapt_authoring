apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - mongo-service.yaml

patches:
  - path: mongo-init.yaml
  - path: migration-init.yaml
  - path: app-init.yaml

labels:
  - includeSelectors: true
    pairs:
      environment: dev
