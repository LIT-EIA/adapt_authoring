apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - mongo-service.yaml

patchesStrategicMerge:
  - mongo-init.yaml
  - migration-init.yaml

labels:
  - includeSelectors: true
    pairs:
      environment: dev
