# kustomize:patch
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  template:
    spec:
      containers:
        - name: app
          workingDir: /app
          command: ["npm", "start"]
          volumeMounts:
            - name: storage
              mountPath: /app/storage
